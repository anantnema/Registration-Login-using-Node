# Registration-Login-using-Node
Nodejs, express, bcrypt, passport authentication, mongodb, mongoose

How to run it -> 
1) clone it
2) npm install
3) node app.js
4) localhost:3000

(Registration is working fine with the database, login has some bug as there is some bug in the bcrypt package)
